#include <stdio.h>

/**
 * main - Prints a specific string followed by a new line
 *
 * Description: Uses printf to display "with proper grammar, but the outcome is
 *              a piece of art," followed by a newline.
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
